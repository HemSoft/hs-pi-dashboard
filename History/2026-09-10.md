
## 02:52 - Authored air agent install prompt
Air's agent never came back after the reboot (port 8787 refused; machine
reachable). Wrote a paste-ready prompt for the Pi session on air to install
the Taildropped binary + LaunchAgent plist and verify health. New deploy/
plist and README section added earlier today.

## 03:12 - Fixed home-dir session labels, deployed fleet-wide
Air sessions titled "home" (pi launched from /Users/home) read like another
machine. Scanner now labels home-dir sessions "~" (test added). Deployed:
mini agent+serve via SSH, home agent via HKCU Run-key binary swap, air binary
Taildropped (swap pending on air). Rebuilt all three dist binaries.
