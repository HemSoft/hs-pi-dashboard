# 2026-09-08

## 08:58 - Published Hermes sessions integration

Ran repo-cleanup Clean on main: committed the Hermes SQLite session provider
(`internal/hermes`), the `GET /api/hermes-sessions` endpoint, the
`modernc.org/sqlite` dependency, and the session-cards-to-table UI redesign,
then pushed origin/main. Planned verification: go vet / test / staticcheck /
deadcode (CI parity) via `-ValidationScript` before push. Reusable improvement:
`repo-cleanup.ps1` must be invoked through pwsh 7 with an explicit Windows
path — Windows PowerShell 5.1 fails its `#requires`, and Git Bash's `$HOME`
mangles the path into a POSIX form pwsh can't resolve.

## 09:09 - Fixed CI: setup-go pin SHA had a one-character typo

CI had never passed — every run since the v0.1 push died in ~5s at GitHub's
action-resolution phase: the pinned `actions/setup-go` SHA ended `…303a` but
upstream v7.0.0 is `…303e`. Corrected the final hex digit and added
`scripts/check-action-pins.sh` + a `pin-check` workflow as a regression guard
(it verifies every `uses:` pin against the real upstream tag SHA from outside
the resolution phase, which no step inside ci.yml can do). Confirmed
diagnosis: transcription typo, not a moved tag or renamed repo.

## 11:27 - Session table: started + duration columns replace token counts

Dropped the ↑in↓out tokens column and its helper; added **started** (wall
clock pinned to America/New_York, date prefix for off-day starts, ISO in the
tooltip) and **duration** (active sessions count live to now on each 5s
render, ended/idle measure lastActivity−startedAt, reusing compactDur). The
old runtime column (relative start age) was superseded by started. Title
column narrowed to make room. Helper logic verified in node before publish;
Full validation gates green locally.

## 11:37 - Started column now shows a compact age

Switched the session table's started column from absolute Eastern clock time
to the same compact format as duration (42m / 5h 4m / 6d 18h), live-ticking
on each 5s render; the exact ISO timestamp remains in the cell tooltip.
startedTime()/HOME_TZ helpers removed. Gates green locally; deployed to mini.
